import React from 'react';
import './Settings.css';  // Import CSS for Settings

const Settings = () => {
    return (
        <div className="settings-container">
            <h1>Settings</h1>
            <p>Manage your account and preferences here.</p>
        </div>
    );
};

export default Settings;
